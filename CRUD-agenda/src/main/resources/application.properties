# Servidor y puerto de la aplicación
server.port=8080

# Configuración de la Base de Datos MySQL (Ajusta tu usuario y contraseña)
spring.datasource.url=jdbc:mysql://localhost:3306/agendasena?serverTimezone=UTC&useSSL=false
spring.datasource.username=root
spring.datasource.password=MySQLdejulian1472.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Forzar la ejecución de data.sql si existe
spring.sql.init.mode=always